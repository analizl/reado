class AuthorsController < ApplicationController
  def show
    # TODO: Wikipedia scrapper to look for authors info
    #url = BASE_URL + params[:id]

    #response = client.send_request(method: :get, url: url, event: "SEARCH")
    #@author_info = response.body
  end
end
