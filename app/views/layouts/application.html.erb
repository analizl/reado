<!DOCTYPE html>
<html>
  <head>
    <title>Reado</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <%= favicon_link_tag asset_path('favicon.ico') %>
  </head>

  <body>
    <nav class="bg-white border-gray-200 dark:bg-gray-600">
      <div class="flex flex-wrap justify-between items-center mx-auto max-w-screen-xl p-4">
          <a href=<%= root_path %> class="flex items-center space-x-3 rtl:space-x-reverse">
            <img src=<%= asset_path('favicon.ico') %> class="h-8" alt="ReadO Logo" />
            <span class="self-center text-2xl font-semibold whitespace-nowrap dark:text-white">ReadO</span>
          </a>
          <div class="flex items-center space-x-6 rtl:space-x-reverse">
            <%= form_with url: "/books", method: :get do |form| %>
              <%= form.text_field :title, class:"p-2 ps-10 text-sm text-gray-700 border border-gray-300 rounded-lg bg-gray-50 focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500", value:"&#xf002;".html_safe, placeholder:"Search a book...".html_safe, style: "font-family: 'FontAwesome';" %>
              <%#= form.label :author, "author:" %>
              <%#= form.text_field :author, value: @author_form, class:"p-2 ps-10 text-sm text-gray-700 hover:text-gray-700 hover:bg-gray-500 border border-gray-300 rounded-lg bg-gray-50 focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500", placeholder:"Author..." %>

            <% end %>
            <% if signed_in? %>
              <%= link_to "Salir", destroy_user_session_path, data: { "turbo-method": :delete }, class:"text-sm  text-gray-500 dark:text-white hover:underline" %>
            <% else %>
              <%= link_to "Registrarse", new_user_registration_path, class:"text-sm  text-gray-500 dark:text-white hover:underline" %>
              <%= link_to "Ingresar", new_user_session_path, class:"text-sm  text-gray-500 dark:text-white hover:underline" %>
            <% end %>
          </div>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
